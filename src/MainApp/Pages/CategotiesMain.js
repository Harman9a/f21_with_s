import { View, Text } from 'react-native';
import React from 'react';
import CommingSoon from '../Components/CommingSoon';

const CategotiesMain = () => {
  return (
    <View>
      <CommingSoon text="Categoties" />
    </View>
  );
};

export default CategotiesMain;
